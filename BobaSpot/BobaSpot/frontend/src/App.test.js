import renderer from 'react-test-renderer';
import App from './App';

// may have to use mock
describe('App component', () => {
    it('renders correctly', () => {
        // const app = renderer.create(
        //     <App />
        // );
        // expect(app).toMatchSnapshot();
    });
});
